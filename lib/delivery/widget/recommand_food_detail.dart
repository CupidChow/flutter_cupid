import 'package:flutter/material.dart';

class RecommandFoodDetail extends StatelessWidget {
  const RecommandFoodDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: CustomScrollView(
        slivers: [
          
        ],
      ),
    );
  }
}